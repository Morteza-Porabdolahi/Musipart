const loadMoreBtn=document.querySelector(".load-more"),searchInput=document.querySelector(".search-input"); let clickedCount=0,perClick=10;function handleSearch(e){const t=allResults.filter((t=>e.target.value?t.title.toLowerCase().includes(e.target.value.toLowerCase()):t));songsWrapper.innerHTML="",createHTMLElementsFromData(t)}searchInput.addEventListener("input",handleSearch),loadMoreBtn.addEventListener("click",manipulateData);
