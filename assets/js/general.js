const loadMoreBtn=document.querySelector(".load-more"),searchInputs=document.querySelectorAll(".search-input");let clickedCount=0;const perClick=10;function handleSearch(e){const t=allResults.filter((t=>e.target.value?t.title.toLowerCase().includes(e.target.value.toLowerCase()):t));songsWrapper.innerHTML="",createHTMLElementsFromData(t)}searchInputs.forEach((e=>e.addEventListener("input",handleSearch))),loadMoreBtn.addEventListener("click",manipulateData);
